# nuclei templates collection and a tool to run it all


# Usage

#### Running all templates can be compute intensive

```
python nuclei.py -d example.com
```

#### Find the valid bugs ( grep and ripgrep needed )

```
rg . | grep "200 OK"
```
